{{!
Це код container файлу для index сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
}}
<main class="main">
  {{#with data}}
  <form method="POST" action="/purchase-update?id={{id}}" class="form-card">

    {{>heading title='Зміна даних'}}
    <span>ID: {{id}}</span>

    <div class="data_row">
      {{>field-update label="Ім'я" name=firstname}}
      {{>field-update label="Прізвище " name=lastname}}
      {{>field-update label="Телефон " name=phone}}
      {{>field-update label="Пошта " name=email}}

    </div>
    {{>button text="Зберегти зміни"}}
  </form>
  {{/with}}
</main>





{{!
Цей код містить лише те, що буде виводитись в

<body>
  Адже наш layout/default.hbs має вже структуру HTML
  }}